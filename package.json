{
  "name": "gworkspace-reports-apisentinel-conn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:js": "jest --runInBand",
    "lint:js": "eslint . --ext .js || true",
    "pretest": "npm run lint:js"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "jest": "^29.6.0",
    "nock": "^13.3.0",
    "jest-environment-jsdom": "^29.6.0"
  }
}
