{
  "name": "gworkspace-reports-apisentinel-conn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:js": "jest --runInBand --coverage",
    "coverage:js": "jest --coverage --coverageDirectory=coverage/js",
    "lint:js": "eslint . --ext .js --max-warnings 0",
    "pretest": "npm run lint:js"
  },
  "devDependencies": {
    "eslint": "^8.50.0",
    "jest": "^29.6.0",
    "jest-environment-jsdom": "^29.6.0",
    "jest-junit": "^13.2.0",
    "jshint": "^2.13.6",
    "nock": "^13.3.0"
  }
}
